# Tests

Run the tests using the following command:

```bash
../run-coverage.ps1
.powershell -ExecutionPolicy Bypass -File ./run-coverage.ps1
```

This will execute all unit tests and generate a code coverage report in the `coveragereport` directory. Open [coveragereport/index.html](coveragereport/index.html) to view the coverage details.